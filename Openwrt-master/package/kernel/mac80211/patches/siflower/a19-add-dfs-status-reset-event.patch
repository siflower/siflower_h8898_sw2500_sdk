Index: backports-5.15.162-1/include/uapi/linux/nl80211.h
===================================================================
--- backports-5.15.162-1.orig/include/uapi/linux/nl80211.h
+++ backports-5.15.162-1/include/uapi/linux/nl80211.h
@@ -6288,6 +6288,7 @@ enum nl80211_smps_mode {
  *	applicable for ETSI dfs domain where pre-CAC is valid for ever.
  * @NL80211_RADAR_CAC_STARTED: Channel Availability Check has been started,
  *	should be generated by HW if NL80211_EXT_FEATURE_DFS_OFFLOAD is enabled.
+ * @NL80211_RADAR_DFS_STATUS_RESET: Reset channel dfs status to USABLE.
  */
 enum nl80211_radar_event {
 	NL80211_RADAR_DETECTED,
@@ -6296,6 +6297,7 @@ enum nl80211_radar_event {
 	NL80211_RADAR_NOP_FINISHED,
 	NL80211_RADAR_PRE_CAC_EXPIRED,
 	NL80211_RADAR_CAC_STARTED,
+	NL80211_RADAR_DFS_STATUS_RESET,
 };
 
 /**
Index: backports-5.15.162-1/net/wireless/mlme.c
===================================================================
--- backports-5.15.162-1.orig/net/wireless/mlme.c
+++ backports-5.15.162-1/net/wireless/mlme.c
@@ -942,7 +942,7 @@ void cfg80211_cac_event(struct net_devic
 
 	trace_cfg80211_cac_event(netdev, event);
 
-	if (WARN_ON(!wdev->cac_started && event != NL80211_RADAR_CAC_STARTED))
+	if (WARN_ON(!wdev->cac_started && event != NL80211_RADAR_CAC_STARTED && event != NL80211_RADAR_DFS_STATUS_RESET))
 		return;
 
 	if (WARN_ON(!wdev->chandef.chan))
@@ -965,6 +965,9 @@ void cfg80211_cac_event(struct net_devic
 	case NL80211_RADAR_CAC_STARTED:
 		wdev->cac_started = true;
 		break;
+	case NL80211_RADAR_DFS_STATUS_RESET:
+		cfg80211_set_dfs_state(wiphy, chandef, NL80211_DFS_USABLE);
+		break;
 	default:
 		WARN_ON(1);
 		return;
